import { useState, useEffect } from 'react';

const useProductIsMeal = (product) => {
    const [isMeal, setIsMeal] = useState(true);

    useEffect(() => {
        
        
    }, [product])
}

export default useProductIsMeal;